<h1><%= @subject.name %></h1>

<% if user_created?(@subject)  %>
  <div class="button"><a href="/subjects/<%= @subject.slug %>/edit">Edit Subject</a></div>
<% end %>

<% if @subject.description %>
  <p>Description: <%= @subject.description %></p>
<% end %>

<p>Courses: <%= @subject.courses.map {|s| s.name}.join(", ") %></p>
<p>Programs: <%= @subject.programs.uniq.map {|s| s.name}.join(", ") %></p>
<p>Platforms: <%= @subject.platforms.uniq.map {|s| s.name}.join(", ") %></p>

<a href="/users/homepage">My Homepage</a>
